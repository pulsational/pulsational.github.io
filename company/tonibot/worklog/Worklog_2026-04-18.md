## 2026-04-18 worklog - part 1
### Work task 1: P5-1 统一 FreeMessageStore 消费端逻辑
#### Overview
SanctuaryView 和 ScriptureChatView 各自写了一套 hasReachedFreeLimit / freeMessagesRemaining 等逻辑，重复且有分歧（ScriptureChatView 缺首次 session 20 条上限 + 999 sentinel 残留）。把判断/计算统一抽到 FreeMessageStore 里。
#### Work log
- 读 FreeMessageStore、SanctuaryView、ScriptureChatView 全量代码分析重复模式
- 发现 2 个实际 bug：ScriptureChatView 缺 first-session 20 条 cap + 999 sentinel
- 新增 3 个统一方法到 FreeMessageStore：hasReachedLimit()、footerState()、shouldRecordSession()
- 移动 FreeMessageFooter enum 从 SanctuaryView 到 FreeMessageStore
- SanctuaryView/ScriptureChatView 改为 1 行 thin wrapper
- 更新 FreeMessageFooterTests（9 处 SanctuaryView→FreeMessageStore + 4 新 hasReachedLimit 测试）
- 80 tests 全绿，commit d9deed5
#### Related information
- Branch: p5-unified-limits
- Commit: d9deed5
- Reviewer: TiaoBot (APPROVE)
#### Summary for next session
P5-1 已 merge 到 master。FreeMessageStore 是所有限额逻辑的单一入口，后续改限额只需改 FreeMessageStore。

### Work task 2: P5-2 统一 LLM 流式处理器
#### Overview
SanctuaryViewModel 和 ScriptureChatViewModel 有 ~130 行重复的流式生成/压缩/假消息检测代码。抽取到共享 StreamingHandler.swift。
#### Work log
- 读两个 ViewModel 全量代码（391 + 540 行），分析 4 类重复模式
- 设计 closure-based delegation 方案（因 ChatMessage vs ScriptureChatMessage 类型不同）
- 新建 StreamingHandler.swift，封装 4 个方法：waitForModelReady、compactIfNeeded、processStream、stripRolePrefix
- 重构 SanctuaryViewModel.confess()：compaction + streaming 全部走 StreamingHandler
- 重构 ScriptureChatViewModel：sendMessage() + startWithTodayScripture() 两处走 StreamingHandler
- 删除 ScriptureChatViewModel.stripRolePrefix 私有方法
- 需要 xcodegen generate 才能把新文件加入 pbxproj
- 10 个新 StreamingHandler 测试，90 tests 全绿，commit 5136c0a
#### Related information
- Branch: p5-streaming
- Commit: 5136c0a
- Reviewer: TiaoBot (APPROVE)
#### Summary for next session
StreamingHandler 是所有 LLM 流式处理的共享入口。新增 ViewModel 时不需要再重复写 for-await 循环。

### Work task 3: P5-4+P5-6 Review（TuanBot 交付）
#### Overview
Review TuanBot 的 Onboarding 流式提取 + it.lproj 旧版 prompt 清理。
#### Work log
- 独立 git show 核查 3 个文件 diff
- LLMService.streamSimple() thin wrapper 正确
- OnboardingView 36→24 行简化正确
- it.lproj 3 个 prompt（intercession/distress/gratitude）与 EN 逐字一致
- APPROVE merge
#### Related information
- Commit: 1847608
#### Summary for next session
无特殊后续。

### Work task 4: P6-1 递减式免费额度 + 防时间篡改
#### Overview
把终身 7 session 硬上限改为递减式每日额度（Day 1-7 unlimited → 8-14 5条 → 15-21 3条 → 22+ 2条），加防改时间机制。
#### Work log
- 读当前 FreeMessageStore + 所有消费端引用
- 新增 3 个 Keychain key：absolvo_total_days_used、absolvo_last_seen_date、absolvo_first_use_date
- 新增 getString/setString Keychain 字符串读写方法
- 新增 recordDailyVisit()、isClockTamperedToday、dailyMessageLimit(forDaysUsed:clockTampered:) 纯函数
- messagesPerSession 改为 computed property
- 保留向后兼容层：hasExhaustedFreeSessions=false、isFirstSession=isUnlimitedPeriod、maxLifetimeSessions=7
- hasReachedLimit() 新增 dailyLimit 参数
- footerState() 简化（unlimited period 始终返回 .unlimited）
- 修 DevToolsResetTests 适配 isFirstSession 语义变更
- 25 个新测试，102 tests 全绿，commit 36f1bc0
#### Related information
- Branch: p6-tiered-limits
- Commit: 36f1bc0
- Reviewer: TiaoBot (APPROVE)
- Follow-up: InnerRoomApp.init 需加 recordDailyVisit()（TuanBot 补了）
#### Summary for next session
递减式限额已上线。消费端零改动。SanctuaryView 的 lifetimeExhausted 分支是死代码（P6-4 清理）。

### Work task 5: P7-2 P0 隐私 bug Review（TuanBot 交付）
#### Overview
Reset All 只清 Keychain 不清 CoreData 祷告记录，用户忏悔内容 reset 后仍可见。
#### Work log
- 独立 git show 核查 FreeMessageStore.resetAll() diff
- 确认 PersistenceService.deleteAllPrayers() + PausedConversationManager.clearAll() 正确
- Task { @MainActor in } actor isolation 正确
- APPROVE merge（紧急）
#### Related information
- Commit: 3d9b601
#### Summary for next session
P0 已修复。

### Work task 6: P6-5 Tech Debt Review（TuanBot 交付）
#### Overview
LibLlama.swift C string interop TODO 修复。
#### Work log
- 核查 String(cString: result) 替代手工 UnicodeScalar 逐字节转换
- 确认修了潜在 UTF-8 多字节乱码 bug
- APPROVE merge
#### Related information
- Commit: a48d39b
#### Summary for next session
Sources/ 和 Tests/ 无 TODO/FIXME/HACK/XXX。

### Work task 7: P7-1 XCUITest target + 递减额度 E2E 测试
#### Overview
从零建 InnerRoomUITests target，写 30 个 E2E 测试（3 人并行写），在模拟器上校准。
#### Work log
- 新建 InnerRoomUITests target（project.yml + xcodegen）
- 给 SanctuaryView 加 7 个 accessibility identifier（input/send/counter/paywall/prayer-type）
- 给 SettingsView 加 7 个 accessibility identifier（Dev Tools 面板）
- 给 ScriptureChatView 加 settings-gear-button identifier
- 写 8 个 TieredLimitsUITests
- 合入 TuanBot 15 个测试（OnboardingUITests + RegressionUITests）
- 合入 TiaoBot 7 个测试（ConfessionPromptUITests + ScriptureChatLimitUITests）
- 发现自定义 SacredTabBar 导航问题（非标准 TabView，Settings 是 gear icon sheet）
- 修全部 5 个测试文件的导航逻辑
- 添加 -UITesting/-resetAll/-setDay=N launch argument 机制
- 修 InnerRoomApp.init 顺序（resetAll → recordDailyVisit → setDay）
- 修 ContentView 支持跳过 Onboarding
- TieredLimitsUITests 8/8 全绿
- RegressionUITests 4/7 通过
- 磁盘满中断一次（14GB 清理后恢复）
- 校准工作交接给 TiaoBot 继续
#### Related information
- Branch: p7-uitests
- Commits: 9581f62 → 2d03fd0 → 540a4b9 → 3d9bc28
#### Summary for next session
TieredLimitsUITests 已校准通过。RegressionUITests 3 个测试需微调（Daily Bread 内容检测、unlimited counter 断言、消息发送超时）。OnboardingUITests 和 ConfessionPromptUITests 未跑。TiaoBot 接手继续。Launch argument 机制（-resetAll/-setDay=N）是可靠的 UI 测试状态控制方式。
