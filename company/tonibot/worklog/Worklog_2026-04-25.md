## 2026-04-25 worklog - part 1
### Work task 1: 双指示器/自动滚动/重复回复 bug 修复
#### Overview
浣熊王反复报告的三个顽固 bug：双等待符、自动滚动失败、回复内容重复。经过彻底根因分析后一次性修复。
#### Work log
- 双指示器：ModelStatusIndicator 只在 tab 0-1 渲染（物理删除，不是条件隐藏）+ 删除死代码 TypingIndicator wrapper
- 自动滚动：TiaoBot 发现 DragGesture `height > 50` 方向反了（向下触发而非向上）+ sendConfession 开头重置 userScrolledUp
- 重复回复：getWarningResponse 返回 originalResponse + supportNote 导致重复 → 只返回 supportNote
- repeat_penalty last_n 128→256 覆盖跨段落重复
- 祷告中隐藏 tab bar + safeAreaInset 56pt 去掉
#### Related information
- Commits: 8fbcaee, 656fcd7, 17bb3d4, 5ab03cf, b9a9b7b, f279980, eb799f0
#### Summary for next session
双指示器彻底解决（物理删除），滚动 DragGesture 方向修正。

### Work task 2: 祷告 prompt 固定建议列表删除 + PostProcessor 追问池扩大
#### Overview
模型每次都选同一个建议（"写封信"），PostProcessor 追问只有 1 个。删固定列表 + 扩大追问池到每类 5 个 × 7 语言 = 175 条。
#### Work log
- 忏悔/代祷/感恩 prompt 删固定建议例子（EN + IT）
- PostProcessor appendFollowUpQuestion 改为从 5 个池随机选 + 上下文关键词评分
- 150 条追问翻译（zh-Hans/zh-Hant/fr/es/pt-BR/it）
- confession.3 "有没有被牵连的人" 改为更通用的 "这件事之后你和他之间有什么变化吗"
#### Related information
- Commits: 2ec47c1, c37a797, 80e46b0
#### Summary for next session
PostProcessor 追问现在是上下文感知的（按 AI 回复关键词评分选最相关的），不再纯随机。

### Work task 3: TopicScripture 映射表 — 话题 × 祷告类型二维经文推荐
#### Overview
RAG 搜索返回不相关经文。建立话题→经文预设映射，14 个基础话题 × 4 祷告类型各 2-3 段经文，匹配时跳过 RAG。浣熊王要求扩展到 40-50 话题。
#### Work log
- 创建 TopicScripture.swift，14 话题 × 单类型映射
- 重构为 4 祷告类型二维映射（同一话题忏悔/代祷/困苦/感恩各有不同经文）
- 集成到 SanctuaryViewModel Turn 3+：匹配话题 → 注入经文提示 → 跳过 RAG
- 饶恕话题太 6:14-15 改弗 4:32（安慰而非威胁）
- 正在扩展到 40-50 话题（关系/身份 + 生活/灵性 + 生命阶段）
#### Related information
- Commits: 461c2f3, 7f971cd
- 扩展进行中（TiaoBot/TuanBot 各贡献 15 话题）
#### Summary for next session
TopicScripture 正在从 14 扩到 40-50 话题。三人各负责一个方向。需要统一合入。

### Work task 4: 后台推理冻屏修复 + GPU 加速恢复
#### Overview
LLM 推理中切后台再切回 app 冻住。同时发现真机一直用 CPU 推理（n_gpu_layers 没设）。
#### Work log
- TiaoBot 加 cancelAllGeneration（后台取消推理）+ resetGeneratingStateIfStale（回来重置）
- 我补了 LLMService.isGenerating @Published 属性（TiaoBot 漏了声明）
- 恢复 n_gpu_layers=99（旧 framework 不支持 Metal 自动 fallback CPU）
#### Related information
- Commits: 145dce0, c1c81dc
#### Summary for next session
后台推理取消机制就位。GPU 加速恢复但旧 framework 可能不支持 Metal。

### Work task 5: 竞品调研 + 通知系统审计
#### Overview
深度调研 Hallow/Pray.com/Glorify/Abide/YouVersion/Lectio 365 等竞品。审计 InnerRoom 现有通知系统。
#### Work log
- 发现隐私是最大市场空白——没有竞品做端侧私密 AI
- 发现 NotificationService.swift 已完整实现（230 行），之前审计说"只是 UI 壳"是错的
- 通知功能完整：每日经文提醒 + 3 天回访提醒 + Onboarding 集成
#### Related information
- 市场规模 $25.2B (2025) → $61.2B (2032)
#### Summary for next session
通知已完整可用。InnerRoom 的核心差异化是端侧隐私 AI。

### Work task 6: Best-of-N + ResponseScorer + 打字模拟 + maxTokens 优化
#### Overview
Turn 1-2 生成 2 个候选回复，用规则评分选最好的。加逐字弹出打字模拟。maxTokens 512→150 加速。
#### Work log
- 创建 ResponseScorer.swift（评分规则：问号/长度/镜像/禁词/经文检测）
- LLMService.generateComplete() 复用 streaming pipeline 的非流式接口
- SanctuaryViewModel Turn 1-2 走 Best-of-N → 评分选优 → PostProcessor → 逐字弹出
- maxTokens 参数化，Turn 1-2 传 150（2×150 比 1×512 更快）
- 逐字弹出 15-40ms per char + streamingTokenCount 触发 auto-scroll
#### Related information
- Commits: 1f6072b, 8fe10bf, 5a372d5
#### Summary for next session
Best-of-N 系统就位。Turn 1-2 质量显著提升（选优），Turn 3+ 保持流式。

### Work task 7: ResponseStarters 测试 + LLMService isGenerating 补全
#### Overview
补全 ResponseStarters 单元测试 + 修 LLMService.isGenerating 声明缺失。
#### Work log
- 2 个 ResponseStarters 测试（非空输出 + 防连续重复）
- LLMService 补 @Published isGenerating 属性 + token gen 开始/结束设值
#### Related information
- Commits: f279980, c1c81dc
#### Summary for next session
113 tests 全绿。新增 ResponseScorer(5) + PostProcessor(4) + DynamicFewShot(4) + ResponseStarters(2) = 15 个测试。
