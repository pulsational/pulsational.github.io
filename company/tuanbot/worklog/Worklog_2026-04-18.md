# TuanBot Worklog

## 2026-04-18 worklog - part 1
### Work task 1: P6 Tiered Free Limits + Dev Tools
#### Overview
Supported the rollout of the new tiered free-message system (P6-1 through P6-5). Performed merge + push operations for all P6 tasks, ran tests at each stage, and handled ghost pbxproj stashing per protocol.

#### Work log
- Merged P6-1 (tiered free limits: unlimited->5->3->2/day + anti-clock-tampering) + wired `recordDailyVisit()` in `InnerRoomApp.swift` init. Commit `75c6453`, pushed.
- Merged P6-2 (Dev Tools tiered limits testing panel) after reviewing TiaoBot's code. Commit `44dc5db`, pushed.
- Reviewed P6-3 (Day 1-7 cap 999->20). APPROVE. Merged commit `7b343b9`, pushed.
- Implemented P6-5 (LibLlama tech debt): replaced manual char-by-char C string interop with idiomatic `String(cString:)`. Commit `a48d39b`. Reviewed by ToniBot, APPROVE.
- Reviewed P6-4 (dead code cleanup, net -163 lines). APPROVE.
- Merged P6-4 + P6-5 sequentially, pushed. Final commit `81da36c`.

#### Related information
- All P6 tasks involved the FreeMessageStore tiered limits system
- Ghost pbxproj stashing protocol followed throughout
- 94-104 tests green at each merge point

### Work task 2: P7 E2E Audit + Bug Fixes
#### Overview
Ran comprehensive E2E simulator audit (79 screenshots) discovering 9 bugs (P0-P3). Then implemented fixes for P0 privacy bug, wrote XCUITest code, and performed code reviews for team fixes.

#### Work log
- **E2E Simulator Audit**: Used `ios-simulator-auditor` agent. 79 screenshots across 15 screens. Found:
  - P0: Reset All doesn't clear CoreData prayer history (privacy breach)
  - P1 x2: Confession passcode blocking + Settings Done button scrolls away
  - P2 x4: Model loading "0%", AI scroll indicator, onboarding reset, Guided Prayer truncation
  - P3 x2: Demo title truncation, welcome icon timing
- **P7-2 (P0 privacy fix)**: Added `PersistenceService.shared.deleteAllPrayers()` + `PausedConversationManager.shared.clearAll()` to `FreeMessageStore.resetAll()`. Used `Task { @MainActor in }` for actor isolation. Commit `3d9b601`. Reviewed by ToniBot, APPROVE. Merged + pushed `1d891ef`.
- **P7-5 (P2 fixes)**: Reset All clears `hasCompletedOnboarding` + Guided Prayer `lineLimit(2->3)`. Commit `a2f95d9`. Reviewed by TiaoBot, APPROVE.
- **P7-A (Accessibility)**: Added 10 `.accessibilityLabel()` to icon-only buttons across SanctuaryView, ScriptureChatView, GuidedPrayerView. Commit `7af69f7`. Reviewed by ToniBot, APPROVE.
- **Reviews performed**: P7-3 (TiaoBot, confession + settings), P7-4 (TiaoBot, loading text + scroll button), P7-6 (TiaoBot, P3 UI fixes). All APPROVE.
- **Merges performed**: P7-2, P7-3, P7-4, P7-5, P7-6, P7-A, P7-T (text truncation audit). All pushed.
- **XCUITest code**: Wrote 15 tests in `OnboardingUITests.swift` + `RegressionUITests.swift` to `/tmp/tuanbot-uitests/`, later merged into p7-uitests by ToniBot.

#### Related information
- Audit screenshots: `/tmp/innerroom-qa-test/` (cleaned for disk space)
- Test scheme changed: `InnerRoom` scheme lost test action after UITest target addition, use `InnerRoomTests` scheme

### Work task 3: P7-P Prompt Ordering + P8 Model Upgrade
#### Overview
Fixed LLMService system prompt concatenation order so FINAL CHECK constraints land at the end (recency bias for 3B model). Then participated in Qwen 3 4B model upgrade decision and execution.

#### Work log
- **P7-P initial fix**: Moved `additionalContext` (containing FINAL CHECK) after RAG + language instruction in both `generate()` and `generateStreaming()`. Commit `99c65b0`, pushed.
- **P7-P follow-up**: Fixed regression where `replaceSystemPrompt=true` path (Scripture Chat) lost RAG + language instruction. Moved RAG + language outside if/else block. Commit `b102788`, pushed.
- **Team discussion**: Recommended Qwen 3 4B over Qwen 3.5 4B from stability angle — 3.5 has known llama.cpp issues (loading failures, KV cache bfloat16 precision loss). Team decided Qwen 3 4B.
- **Model upgrade attempt**: Qwen 3 4B loaded but `llama.xcframework` doesn't support `qwen3` architecture (only `qwen2`). ToniBot rebuilding framework.
- **Rollback aborted**: Started downloading Qwen 2.5 3B for rollback, but 浣熊王 decided to push forward with framework rebuild. Cleaned up — deleted old model, restored LLMService to Qwen3 refs.

#### Related information
- Qwen 3.5 llama.cpp issues: github.com/ggml-org/llama.cpp issues #19879, #20035
- Framework rebuild: ToniBot using `build-xcframework.sh` from llama.cpp repo

### Work task 4: Operations + Cleanup
#### Overview
Multiple rounds of disk space management and git housekeeping due to 2GB gguf model + 4 parallel Claude sessions filling the 228GB drive.

#### Work log
- Cleaned 17 old worktrees (first round, freed 24GB)
- Cleaned 11 ghost-pbxproj stashes + 42 branches (37 merged + 5 stale)
- Multiple rounds of DerivedData cleanup when disk filled during builds
- Final state: 2 worktrees (master + p7-uitests), 2 branches, 0 stashes
- Branch audit: confirmed `p2-model` was the only unmerged branch (content already in master via rebase)

#### Summary for next session
- llama.xcframework rebuild in progress — ToniBot compiling latest llama.cpp for iOS with qwen3 architecture support
- Once framework is ready: replace xcframework → build → test Qwen 3 4B model → push
- If Qwen 3 4B works well, the StreamingHandler think tag filter (already implemented) handles `<think>` block stripping
- `InnerRoom` scheme test action is broken (use `InnerRoomTests` scheme) — needs xcscheme fix
- 30 XCUITest E2E tests compiled but need simulator calibration
- Disk space is a recurring issue — consider `SYMROOT` or post-build DerivedData cleanup hook
